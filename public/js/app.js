const $=s=>document.querySelector(s), api=async(u,o={})=>{const r=await fetch(u,{headers:{"Content-Type":"application/json"},...o});let d={};try{d=await r.json()}catch{}if(!r.ok)throw Error(d.error||"Request failed");return d}, money=n=>"₹"+Number(n||0).toLocaleString("en-IN",{maximumFractionDigits:2});
let currentUser=null,page="dashboard",cache={products:[],customers:[],suppliers:[],expenses:[],invoices:[]};

async function boot(){const d=await api("/api/auth/me");if(!d.user)return login();currentUser=d.user;layout();load("dashboard")}
function login(){app.innerHTML=`<div class="auth"><div class="auth-card"><div class="logo"><div class="logo-icon">B</div><div><strong>BizFlow</strong><small>Business Manager</small></div></div><h1>Welcome back</h1><p>Sign in to manage billing, inventory, customers and business analytics.</p><div id="authErr" class="error"></div><div class="field"><label>EMAIL</label><input id="email" value="admin@bizflow.local"></div><div class="field"><label>PASSWORD</label><input id="password" type="password" value="admin123"></div><button class="btn full" onclick="doLogin()">Sign in</button><p style="text-align:center;margin-top:18px">Demo login: admin@bizflow.local / admin123</p></div></div>`}
async function doLogin(){try{const d=await api("/api/auth/login",{method:"POST",body:JSON.stringify({email:$("#email").value,password:$("#password").value})});currentUser=d.user;layout();load("dashboard")}catch(e){$("#authErr").textContent=e.message}}
function layout(){app.innerHTML=`<div class="layout"><aside class="sidebar"><div class="brand"><div class="logo-icon">B</div><div><strong>BizFlow</strong><small>Business Manager</small></div></div><nav class="nav">${[["dashboard","⌂","Dashboard"],["billing","▣","Billing"],["products","▤","Products"],["customers","♙","Customers"],["suppliers","♙","Suppliers"],["purchases","⇩","Purchases"],["expenses","₹","Expenses"],["reports","◔","Reports"],["settings","⚙","Settings"]].map(x=>`<button id="nav-${x[0]}" onclick="load('${x[0]}')">${x[1]} <span>${x[2]}</span></button>`).join("")}</nav><div class="side-foot"><span>●</span> Business Plan<br>All features enabled</div></aside><main class="main"><header class="top"><div class="search">⌕ <input placeholder="Search..." onkeydown="globalSearch(event)"></div><div class="user"><span>${currentUser.name}</span><div class="avatar">${currentUser.name.slice(0,2).toUpperCase()}</div><button class="btn light" onclick="logout()">Logout</button></div></header><section id="view"></section></main></div><div id="modal" class="modal hidden"><div class="modal-card"><div class="modal-head"><h3 id="mtitle"></h3><button onclick="closeModal()">×</button></div><div id="mbody"></div></div></div>`}
async function load(p){page=p;document.querySelectorAll(".nav button").forEach(x=>x.classList.remove("active"));$("#nav-"+p)?.classList.add("active");try{if(p==="dashboard")return dashboard();if(p==="billing")return billing();if(p==="products"){cache.products=await api("/api/products");return products()}if(p==="customers"){cache.customers=await api("/api/customers");return customers()}if(p==="suppliers"){cache.suppliers=await api("/api/suppliers");return suppliers()}if(p==="purchases"){cache.suppliers=await api("/api/suppliers");cache.products=await api("/api/products");return purchases()}if(p==="expenses"){cache.expenses=await api("/api/expenses");return expenses()}if(p==="reports")return reports();return settings()}catch(e){$("#view").innerHTML=`<div class="content"><div class="alert">${e.message}</div></div>`}}
const head=(title,sub,button="")=>`<div class="content"><div class="head"><div><h1>${title}</h1><div class="sub">${sub}</div></div>${button}</div>`;
async function dashboard(){const d=await api("/api/dashboard");$("#view").innerHTML=head("Dashboard","Overview of your business performance.",`<button class="btn" onclick="invoiceModal()">+ Create Invoice</button>`)+`<div class="cards"><div class="card"><div class="label">TOTAL SALES</div><div class="number">${money(d.sales)}</div><div class="green">Sales revenue</div></div><div class="card"><div class="label">PURCHASES</div><div class="number">${money(d.purchases)}</div></div><div class="card"><div class="label">RECEIVABLES</div><div class="number">${money(d.receivables)}</div></div><div class="card"><div class="label">STOCK VALUE</div><div class="number">${money(d.stock)}</div></div></div><div class="grid"><div class="panel"><div class="panel-head"><h3>Sales Overview</h3><span class="sub">Current data</span></div><div class="bars">${[45,60,48,73,58,88,68].map((h,i)=>`<div class="bar" style="height:${h}%"><small>${["Apr","May","Jun","Jul","Aug","Sep","Oct"][i]}</small></div>`).join("")}</div></div><div class="panel"><h3>Sales by Category</h3><div class="donut"></div><div style="text-align:center;font-size:11px;color:#8791a0">Grocery 55% · Food 23% · Other 22%</div></div></div><div class="grid"><div class="panel"><div class="panel-head"><h3>Recent Invoices</h3><button class="btn light" onclick="load('billing')">View all</button></div>${invoiceTable(d.recent)}</div><div class="panel"><h3>Low Stock</h3>${d.low.length?d.low.map(p=>`<div style="display:flex;justify-content:space-between;padding:12px 0;border-bottom:1px solid #eee"><span>${p.name}<small style="display:block;color:#929baa">${p.sku}</small></span><span class="pill bad">${p.stock} left</span></div>`).join(""):`<p class="sub">No low-stock products.</p>`}</div></div></div>`}
function invoiceTable(a){return `<div class="table-wrap"><table class="table"><thead><tr><th>INVOICE</th><th>CUSTOMER</th><th>DATE</th><th>AMOUNT</th><th>STATUS</th></tr></thead><tbody>${a.map(x=>`<tr><td><b>${x.invoice_no||x.no}</b></td><td>${x.customer||"-"}</td><td>${x.date}</td><td><b>${money(x.total||x.amount)}</b></td><td><span class="pill ${x.status==="Overdue"?"bad":x.status==="Pending"?"warn":""}">${x.status}</span></td></tr>`).join("")}</tbody></table></div>`}
async function billing(){cache.invoices=await api("/api/invoices");$("#view").innerHTML=head("Billing & Invoices","Create and track sales invoices.",`<button class="btn" onclick="invoiceModal()">+ Create Invoice</button>`)+`<div class="cards"><div class="card"><div class="label">TOTAL INVOICES</div><div class="number">${cache.invoices.length}</div></div><div class="card"><div class="label">PENDING</div><div class="number">${money(cache.invoices.filter(x=>x.status==="Pending").reduce((a,x)=>a+x.total,0))}</div></div><div class="card"><div class="label">PAID</div><div class="number">${money(cache.invoices.filter(x=>x.status==="Paid").reduce((a,x)=>a+x.total,0))}</div></div><div class="card"><div class="label">SALES</div><div class="number">${money(cache.invoices.reduce((a,x)=>a+x.total,0))}</div></div></div><div class="panel" style="margin-top:16px"><div class="toolbar"><input placeholder="Search invoice/customer..." oninput="filterRows(this,'invoiceRows')"><button class="btn light">Export</button></div><div id="invoiceRows">${invoiceTable(cache.invoices)}</div></div></div>`}
async function products(){renderProducts(cache.products)}
function renderProducts(a){$("#view").innerHTML=head("Products & Inventory","Manage stock, prices and product information.",`<button class="btn" onclick="productModal()">+ Add Product</button>`)+`<div class="cards"><div class="card"><div class="label">PRODUCTS</div><div class="number">${a.length}</div></div><div class="card"><div class="label">STOCK VALUE</div><div class="number">${money(a.reduce((s,p)=>s+p.price*p.stock,0))}</div></div><div class="card"><div class="label">LOW STOCK</div><div class="number">${a.filter(p=>p.stock<=p.min_stock).length}</div></div><div class="card"><div class="label">CATEGORIES</div><div class="number">${new Set(a.map(p=>p.category)).size}</div></div></div><div class="panel" style="margin-top:16px"><div class="toolbar"><input placeholder="Search products..." oninput="renderProducts(cache.products.filter(p=>(p.name+p.sku+p.category).toLowerCase().includes(this.value.toLowerCase())))"></div><div class="table-wrap"><table class="table"><thead><tr><th>PRODUCT</th><th>SKU</th><th>CATEGORY</th><th>PRICE</th><th>COST</th><th>STOCK</th><th>ACTION</th></tr></thead><tbody>${a.map(p=>`<tr><td><b>${p.name}</b></td><td>${p.sku}</td><td>${p.category}</td><td>${money(p.price)}</td><td>${money(p.cost)}</td><td><span class="pill ${p.stock<=p.min_stock?"bad":""}">${p.stock}</span></td><td><button class="btn light" onclick="editProduct(${p.id})">Edit</button> <button class="btn red" onclick="deleteProduct(${p.id})">Delete</button></td></tr>`).join("")}</tbody></table></div></div></div>`}
async function customers(){renderCustomers(cache.customers)}
function renderCustomers(a){$("#view").innerHTML=head("Customers","Manage customer accounts and outstanding balances.",`<button class="btn" onclick="customerModal()">+ Add Customer</button>`)+`<div class="cards"><div class="card"><div class="label">CUSTOMERS</div><div class="number">${a.length}</div></div><div class="card"><div class="label">RECEIVABLES</div><div class="number">${money(a.reduce((s,x)=>s+x.balance,0))}</div></div></div><div class="panel" style="margin-top:16px"><div class="toolbar"><input placeholder="Search customers..." oninput="renderCustomers(cache.customers.filter(c=>(c.name+c.phone).toLowerCase().includes(this.value.toLowerCase())))"></div><div class="table-wrap"><table class="table"><thead><tr><th>NAME</th><th>PHONE</th><th>EMAIL</th><th>BALANCE</th><th>ACTION</th></tr></thead><tbody>${a.map(c=>`<tr><td><b>${c.name}</b></td><td>${c.phone||"-"}</td><td>${c.email||"-"}</td><td>${money(c.balance)}</td><td><button class="btn red" onclick="deleteCustomer(${c.id})">Delete</button></td></tr>`).join("")}</tbody></table></div></div></div>`}
async function suppliers(){const a=cache.suppliers;$("#view").innerHTML=head("Suppliers","Manage supplier contacts and payables.",`<button class="btn" onclick="supplierModal()">+ Add Supplier</button>`)+`<div class="cards"><div class="card"><div class="label">SUPPLIERS</div><div class="number">${a.length}</div></div><div class="card"><div class="label">PAYABLES</div><div class="number">${money(a.reduce((s,x)=>s+x.balance,0))}</div></div></div><div class="panel" style="margin-top:16px"><table class="table"><thead><tr><th>SUPPLIER</th><th>PHONE</th><th>EMAIL</th><th>BALANCE</th><th>ACTION</th></tr></thead><tbody>${a.map(s=>`<tr><td><b>${s.name}</b></td><td>${s.phone||"-"}</td><td>${s.email||"-"}</td><td>${money(s.balance)}</td><td><button class="btn red" onclick="deleteSupplier(${s.id})">Delete</button></td></tr>`).join("")}</tbody></table></div></div>`}
async function purchases(){const a=await api("/api/purchases");$("#view").innerHTML=head("Purchases","Record purchase bills and increase inventory.",`<button class="btn" onclick="purchaseModal()">+ Record Purchase</button>`)+`<div class="panel"><table class="table"><thead><tr><th>BILL</th><th>SUPPLIER</th><th>DATE</th><th>TOTAL</th><th>STATUS</th></tr></thead><tbody>${a.map(x=>`<tr><td>${x.bill_no}</td><td>${x.supplier}</td><td>${x.date}</td><td>${money(x.total)}</td><td><span class="pill warn">${x.status}</span></td></tr>`).join("")}</tbody></table></div></div>`}
async function expenses(){const a=cache.expenses;$("#view").innerHTML=head("Expenses","Track operating expenses.",`<button class="btn" onclick="expenseModal()">+ Add Expense</button>`)+`<div class="cards"><div class="card"><div class="label">TOTAL EXPENSES</div><div class="number">${money(a.reduce((s,x)=>s+x.amount,0))}</div></div></div><div class="panel" style="margin-top:16px"><table class="table"><thead><tr><th>EXPENSE</th><th>CATEGORY</th><th>DATE</th><th>AMOUNT</th><th>ACTION</th></tr></thead><tbody>${a.map(x=>`<tr><td>${x.name}</td><td>${x.category}</td><td>${x.date}</td><td>${money(x.amount)}</td><td><button class="btn red" onclick="deleteExpense(${x.id})">Delete</button></td></tr>`).join("")}</tbody></table></div></div>`}
async function reports(){const d=await api("/api/reports");$("#view").innerHTML=head("Reports & Analytics","Understand revenue, costs and profitability.","")+`<div class="cards"><div class="card"><div class="label">SALES</div><div class="number">${money(d.sales)}</div></div><div class="card"><div class="label">PURCHASES</div><div class="number">${money(d.purchases)}</div></div><div class="card"><div class="label">EXPENSES</div><div class="number">${money(d.expenses)}</div></div><div class="card"><div class="label">NET PROFIT</div><div class="number ${d.profit>=0?"green":""}">${money(d.profit)}</div></div></div><div class="grid"><div class="panel"><h3>Top Products</h3><table class="table"><thead><tr><th>PRODUCT</th><th>QTY</th><th>REVENUE</th></tr></thead><tbody>${d.top.map(x=>`<tr><td>${x.name}</td><td>${x.qty}</td><td>${money(x.revenue)}</td></tr>`).join("")}</tbody></table></div><div class="panel"><h3>Profit formula</h3><p class="sub">Net Profit = Sales − Purchases − Expenses</p><div class="number">${money(d.profit)}</div></div></div></div>`}
function settings(){$("#view").innerHTML=head("Settings","Business profile and application preferences.","")+`<div class="panel"><div style="padding:14px 0;border-bottom:1px solid #eee"><b>Business Profile</b><p class="sub">Configure business name, address, GSTIN and contact details.</p><button class="btn light">Edit Profile</button></div><div style="padding:14px 0;border-bottom:1px solid #eee"><b>Invoice Settings</b><p class="sub">Invoice numbering, taxes, discounts and templates.</p><button class="btn light">Configure</button></div><div style="padding:14px 0"><b>Data</b><p class="sub">Your local SQLite database is stored in the data folder.</p></div></div></div>`}

function openModal(title,html){$("#mtitle").textContent=title;$("#mbody").innerHTML=html;$("#modal").classList.remove("hidden")}function closeModal(){$("#modal").classList.add("hidden")}
function formField(id,label,value="",type="text"){return `<div class="field"><label>${label}</label><input id="${id}" type="${type}" value="${value}"></div>`}
function productModal(p={}){openModal(p.id?"Edit Product":"Add Product",`<div class="form-grid">${formField("pn","PRODUCT NAME",p.name||"")}${formField("ps","SKU",p.sku||"")}${formField("pc","CATEGORY",p.category||"General")}${formField("pp","SELLING PRICE",p.price||0,"number")}${formField("cost","COST PRICE",p.cost||0,"number")}${formField("stock","STOCK",p.stock||0,"number")}${formField("min","MIN STOCK",p.min_stock||5,"number")}</div><div class="actions"><button class="btn" onclick="saveProduct(${p.id||0})">Save</button></div>`)}
async function saveProduct(id){const b={name:$("#pn").value,sku:$("#ps").value,category:$("#pc").value,price:+$("#pp").value,cost:+$("#cost").value,stock:+$("#stock").value,min_stock:+$("#min").value};try{await api(id?"/api/products/"+id:"/api/products",{method:id?"PUT":"POST",body:JSON.stringify(b)});closeModal();load("products")}catch(e){alert(e.message)}}
async function editProduct(id){productModal(cache.products.find(p=>p.id===id))}
async function deleteProduct(id){if(confirm("Delete this product?")){await api("/api/products/"+id,{method:"DELETE"});load("products")}}
function customerModal(){openModal("Add Customer",`<div class="form-grid">${formField("cn","NAME")}${formField("cp","PHONE")}${formField("ce","EMAIL")}${formField("ca","ADDRESS")}${formField("cb","OPENING BALANCE",0,"number")}</div><div class="actions"><button class="btn" onclick="saveCustomer()">Save</button></div>`)}
async function saveCustomer(){await api("/api/customers",{method:"POST",body:JSON.stringify({name:$("#cn").value,phone:$("#cp").value,email:$("#ce").value,address:$("#ca").value,balance:+$("#cb").value})});closeModal();load("customers")}
async function deleteCustomer(id){if(confirm("Delete customer?")){await api("/api/customers/"+id,{method:"DELETE"});load("customers")}}
function supplierModal(){openModal("Add Supplier",`<div class="form-grid">${formField("sn","NAME")}${formField("sp","PHONE")}${formField("se","EMAIL")}${formField("sa","ADDRESS")}</div><div class="actions"><button class="btn" onclick="saveSupplier()">Save</button></div>`)}
async function saveSupplier(){await api("/api/suppliers",{method:"POST",body:JSON.stringify({name:$("#sn").value,phone:$("#sp").value,email:$("#se").value,address:$("#sa").value})});closeModal();load("suppliers")}
async function deleteSupplier(id){if(confirm("Delete supplier?")){await api("/api/suppliers/"+id,{method:"DELETE"});load("suppliers")}}
function expenseModal(){openModal("Add Expense",`<div class="form-grid">${formField("en","EXPENSE")}${formField("ec","CATEGORY","General")}${formField("ea","AMOUNT",0,"number")}${formField("ed","DATE",new Date().toISOString().slice(0,10),"date")}</div><div class="actions"><button class="btn" onclick="saveExpense()">Save</button></div>`)}
async function saveExpense(){await api("/api/expenses",{method:"POST",body:JSON.stringify({name:$("#en").value,category:$("#ec").value,amount:+$("#ea").value,date:$("#ed").value})});closeModal();load("expenses")}
async function deleteExpense(id){if(confirm("Delete expense?")){await api("/api/expenses/"+id,{method:"DELETE"});load("expenses")}}
function invoiceModal(){if(!cache.customers.length||!cache.products.length){Promise.all([api("/api/customers"),api("/api/products")]).then(([c,p])=>{cache.customers=c;cache.products=p;invoiceModal()});return}openModal("Create Invoice",`<div class="form-grid">${formField("idate","DATE",new Date().toISOString().slice(0,10),"date")}<div class="field"><label>CUSTOMER</label><select id="ic">${cache.customers.map(c=>`<option value="${c.id}">${c.name}</option>`).join("")}</select></div><div class="field"><label>TAX %</label><input id="tax" type="number" value="18"></div><div class="field"><label>DISCOUNT</label><input id="disc" type="number" value="0"></div></div><label style="font-size:12px;font-weight:700">ITEMS</label><div id="lines"></div><button class="btn light" onclick="addLine()">+ Add Item</button><div class="summary" id="sum">Total: ₹0</div><div class="actions"><button class="btn" onclick="saveInvoice()">Create Invoice</button></div>`);addLine()}
function addLine(){const d=document.createElement("div");d.className="invoice-line";d.innerHTML=`<select class="prod">${cache.products.map(p=>`<option value="${p.id}" data-price="${p.price}">${p.name}</option>`).join("")}</select><input class="qty" type="number" value="1" min="1"><input class="price" type="number" value="${cache.products[0]?.price||0}"><button class="btn red" onclick="this.parentElement.remove();calcInvoice()">×</button>`;$("#lines").appendChild(d);d.querySelector(".prod").onchange=()=>{d.querySelector(".price").value=d.querySelector(".prod").selectedOptions[0].dataset.price;calcInvoice()};d.querySelector(".qty").oninput=calcInvoice;d.querySelector(".price").oninput=calcInvoice;calcInvoice()}
function calcInvoice(){let sub=[...document.querySelectorAll(".invoice-line")].reduce((s,l)=>s+(+l.querySelector(".qty").value)*(+l.querySelector(".price").value),0);let dis=+$("#disc")?.value||0,tax=+$("#tax")?.value||0,total=Math.max(0,sub-dis)*(1+tax/100);if($("#sum"))$("#sum").innerHTML=`Subtotal: ${money(sub)} · Tax: ${money(Math.max(0,sub-dis)*tax/100)} · <b>Total: ${money(total)}</b>`}
async function saveInvoice(){const items=[...document.querySelectorAll(".invoice-line")].map(l=>({product_id:+l.querySelector(".prod").value,quantity:+l.querySelector(".qty").value,price:+l.querySelector(".price").value}));try{await api("/api/invoices",{method:"POST",body:JSON.stringify({customer_id:+$("#ic").value,tax_rate:+$("#tax").value,discount:+$("#disc").value,date:$("#idate").value,items})});closeModal();load("billing")}catch(e){alert(e.message)}}
function purchaseModal(){openModal("Record Purchase",`<div class="form-grid"><div class="field"><label>SUPPLIER</label><select id="psup">${cache.suppliers.map(s=>`<option value="${s.id}">${s.name}</option>`).join("")}</select></div>${formField("pdate","DATE",new Date().toISOString().slice(0,10),"date")}</div><div id="plines"></div><button class="btn light" onclick="addPurchaseLine()">+ Add Item</button><div class="actions"><button class="btn" onclick="savePurchase()">Save Purchase</button></div>`);addPurchaseLine()}
function addPurchaseLine(){const d=document.createElement("div");d.className="invoice-line";d.innerHTML=`<select class="pitem">${cache.products.map(p=>`<option value="${p.id}">${p.name}</option>`).join("")}</select><input class="pqty" type="number" value="1" min="1"><input class="pcost" type="number" value="${cache.products[0]?.cost||0}"><button class="btn red" onclick="this.parentElement.remove()">×</button>`;$("#plines").appendChild(d)}
async function savePurchase(){const items=[...document.querySelectorAll("#plines .invoice-line")].map(l=>({product_id:+l.querySelector(".pitem").value,quantity:+l.querySelector(".pqty").value,cost:+l.querySelector(".pcost").value}));try{await api("/api/purchases",{method:"POST",body:JSON.stringify({supplier_id:+$("#psup").value,date:$("#pdate").value,items})});closeModal();load("purchases")}catch(e){alert(e.message)}}
function filterRows(input,id){const q=input.value.toLowerCase();document.querySelectorAll("#"+id+" tbody tr").forEach(r=>r.style.display=r.innerText.toLowerCase().includes(q)?"":"none")}
function globalSearch(e){if(e.key==="Enter"){const q=e.target.value.toLowerCase();if(q.includes("invoice")||q.includes("bill"))load("billing");else if(q.includes("stock")||q.includes("product"))load("products");else if(q.includes("customer"))load("customers");else if(q.includes("expense"))load("expenses");}}
async function logout(){await api("/api/auth/logout",{method:"POST"});currentUser=null;login()}
boot();